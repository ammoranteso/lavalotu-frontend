import { ClientsWrapperComponent } from './clients-wrapper.component';

describe('ClientsWrapperComponent', () => {
  let component: ClientsWrapperComponent;

  beforeEach(() => {
    component = new ClientsWrapperComponent();
  });

  it('should create an instance', () => {
    expect(component).toBeTruthy();
  });
});
